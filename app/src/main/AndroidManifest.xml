<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Keep the CPU alive so playback doesn't stall with the screen off -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Find and play songs anywhere on the phone -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:label="RIP MP3"
        android:icon="@drawable/ic_ripmp3"
        android:roundIcon="@drawable/ic_ripmp3"
        android:theme="@style/Theme.RipMp3">

        <!-- Player is home -->
        <activity
            android:name=".PlayerActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- GET SONGS screen; also receives shared YouTube links -->
        <activity
            android:name=".DownloadActivity"
            android:exported="true"
            android:label="Get Songs"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Library explorer (artists / albums) -->
        <activity
            android:name=".BrowserActivity"
            android:exported="false"
            android:label="Browser" />

        <service
            android:name=".PlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>
</manifest>
